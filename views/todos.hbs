<div class="container">
    <div class="well todos">
        {{> add}}
        <ul>
            {{#each todos}}
            {{> todo this}}
            {{/each}}
        </ul>
        {{>footer}}
    </div>
</div>